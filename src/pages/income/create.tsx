import Layout from "@components/layout"
import { Typography } from "@material-ui/core"

const CreateIncomePage = () => {
    return (
        <Layout>
            <Typography>Create Income Page</Typography>
        </Layout>
    )
}

export default CreateIncomePage
import Layout from "@components/layout"
import { Typography } from "@material-ui/core"

const CreateExpensePage = () => {
    return (
        <Layout>
            <Typography>Create Expense Page</Typography>
        </Layout>
    )
}

export default CreateExpensePage
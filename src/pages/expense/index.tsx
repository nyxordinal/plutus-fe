import Layout from "@components/layout"
import { Typography } from "@material-ui/core"

const ExpensePage = () => {
    return (
        <Layout>
            <Typography>Expense Page</Typography>
        </Layout>
    )
}

export default ExpensePage